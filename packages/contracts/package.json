{
	"name": "@capstone/contracts",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"main": "src/index.ts",
	"types": "src/index.ts",
	"scripts": {
		"build": "tsc --noEmit",
		"check": "tsc --noEmit",
		"test": "vitest run",
		"lint": "prettier --check .",
		"format": "prettier --write ."
	},
	"dependencies": {
		"zod": "^4.1.11"
	},
	"devDependencies": {
		"typescript": "^5.9.3",
		"vitest": "^4.1.0"
	}
}
