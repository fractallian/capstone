# Optional: TLS for the app hostname. Set APP_DOMAIN in `apps/client/.env` (or your env_file).
# Run: docker compose -f docker-compose.yml -f docker-compose.caddy.yml up -d
# Then set ORIGIN=https://$APP_DOMAIN
services:
  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    environment:
      APP_DOMAIN: ${APP_DOMAIN}
    volumes:
      - ./deploy/Caddyfile:/etc/caddy/Caddyfile:ro
    depends_on:
      - app
