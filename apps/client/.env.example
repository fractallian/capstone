# Supabase → Connect (or Project Settings → Database)
#
# Direct host db.<ref>.supabase.co is often IPv6-only. If nslookup fails or Supabase warns “not IPv4
# compatible”, use the Session pooler URI instead (IPv4-friendly, port 5432):
# DATABASE_URL="postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres"
#
# Transaction pooler (port 6543) — serverless-friendly; app uses postgres.js with prepare: false.
# DATABASE_URL="postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres"

DATABASE_URL=""

ORIGIN="http://localhost:5173"
# Docker / production: use your public HTTPS URL for the app.
# ORIGIN="https://app.example.com"
#
# ngrok (or similar): tunnel the Vite dev server (5173). Example:
#   ORIGIN="https://abc123.ngrok-free.app"
# GitHub OAuth callback: {ORIGIN}/api/auth/callback/github — restart `pnpm dev` after changing .env.
# Google OAuth callback: {ORIGIN}/api/auth/callback/google — restart `pnpm dev` after changing .env.

# Better Auth
# For production use 32 characters and generated with high entropy
# https://www.better-auth.com/docs/installation
BETTER_AUTH_SECRET=""

# GitHub OAuth — https://github.com/settings/developers — new OAuth app
# Callback URL must match: {ORIGIN}/api/auth/callback/github (e.g. http://localhost:5173/api/auth/callback/github)
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# Google OAuth — https://console.cloud.google.com/apis/credentials — OAuth 2.0 Client ID (Web application)
# Authorized redirect URI must match: {ORIGIN}/api/auth/callback/google (e.g. http://localhost:5173/api/auth/callback/google)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Local dev only: single-player vs server opponent. Random moves if OPENAI_API_KEY is empty.
AI_OPPONENT_ENABLED="false"
OPENAI_API_KEY=""
# OPENAI_MODEL="gpt-4o-mini"
# Stronger (slower/costlier) play: OPENAI_MODEL="gpt-4o"
# For ngrok’s second tunnel, use wss:// (HTTPS tunnel host), not ws://localhost.
